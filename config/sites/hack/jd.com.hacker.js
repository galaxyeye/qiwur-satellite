// Hack jd.com
if (seajs) {
    seajs.use("WDG_ROOT/commentsList/commentsList2", function(i) {
        i.init();
    });
}

// $('#comments-list .m-tab-trigger a').each(function() {
//     __utils__.echo($(this).attr('href'));
// });
//
// $('.comments-item').each(function () {
//     __utils__.echo($(this).attr('data-guid'));
//     __utils__.echo($(this).text());
// });
